[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "autoprice-ai-pro"
version = "0.1.0"
description = "Production-style Machine Learning project for used car price prediction."
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src"]